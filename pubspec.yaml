name: barrel_generator
description: A sample command-line application with basic argument parsing.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.12.1

dependencies:
  args: ^2.7.0
  freezed: ^3.2.5
  freezed_annotation: ^3.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  build_runner: ^2.15.0
